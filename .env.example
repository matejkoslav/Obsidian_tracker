# Toggl Track API — optional daily work-time import
# Copy this file to .env.local, then paste your token from:
# https://track.toggl.com/profile
TOGGL_API_TOKEN=replace_with_your_toggl_api_token

# Use the timezone for the daily note you want to update.
TOGGL_TIMEZONE=UTC
